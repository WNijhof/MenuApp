services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - menuapp-data:/data
    environment:
      - DATABASE_PATH=/data/menuapp.db

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  menuapp-data:
